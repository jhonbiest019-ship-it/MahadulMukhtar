import 'package:flutter/material.dart';

class AppColors {
  // Official Logo Palette - Royal Navy Blue
  static const Color primary = Color(0xFF19436E);       // Royal Navy Blue
  static const Color primaryDark = Color(0xFF102B48);   // Deep Navy Dark
  static const Color primaryLight = Color(0xFF2563EB);  // Vivid Blue Accent
  static const Color accent = Color(0xFFD97706);        // Warm Gold/Amber

  // Background & Surface
  static const Color background = Color(0xFFF2F5F8);    // Crisp grayish blue
  static const Color cardBg = Colors.white;
  static const Color textPrimary = Color(0xFF1F2937);   // Dark Slate
  static const Color textSecondary = Color(0xFF6B7280); // Muted Gray

  // Attendance Status Colors
  static const Color present = Color(0xFF059669);       // Vivid Emerald Green
  static const Color absent = Color(0xFFDC2626);        // Crimson Red
  static const Color leave = Color(0xFFD97706);         // Amber Gold
  static const Color late = Color(0xFF2563EB);          // Royal Blue

  // UI Accents
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF0F2F1);
  static const Color whatsappGreen = Color(0xFF25D366);  // WhatsApp Brand Color
}
